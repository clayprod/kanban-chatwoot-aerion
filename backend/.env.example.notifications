# Web Push (VAPID) — necessário para push em Chrome/Firefox/Edge/Safari/PWA
# Gerar: npx web-push generate-vapid-keys
# Copie as chaves para backend/.env (não commitar chaves de produção).

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:ops@suaempresa.com
